import React from "react";
import { IObsWebSocket } from "../../obs/renderer";

export interface PreviewProps {
    obs: IObsWebSocket;
}

export const Preview = ({ obs }: PreviewProps) => {
    return <></>;
};