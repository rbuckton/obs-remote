import React from "react";
import { IObsWebSocket } from "../../obs/renderer";

export interface MediaStatusProps {
    obs: IObsWebSocket;
}

export const MediaStatus = ({ obs }: MediaStatusProps) => {
    return <></>;
};