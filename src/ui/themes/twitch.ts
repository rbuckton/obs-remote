import { createMuiTheme } from "@material-ui/core";

export const TwitchTheme = createMuiTheme({
    palette: {
        primary: {
            main: "#9146ff",
            contrastText: "#fff",
        },
        secondary: {
            main: "#fff",
            contrastText: "#9146ff",
        }
    }
});